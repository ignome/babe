<table class="table" width="100%" cellpadding="0" cellspacing="0">
  <thead>
    <tr>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @tags.each do |tag| %>
    <tr>
      <td width="15"><input type="checkbox" name="id[]" vlaue="<%=tag.id%>" /></td>
      <td>
        <%= tag.name %>
      </td>
    </tr>
    <% end %>
  </tbody>
</table>